import React from 'react';
import { Container, Input } from './FocalPointsSearchBarElements';

const FocalPointsSearchBar = () => {
  return (
    <Container>
      <Input type="text" placeholder="Search" />
    </Container>
  );
};

export default FocalPointsSearchBar;
